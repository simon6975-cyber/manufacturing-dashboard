# v2.7 — 실측 DSPM API 연동 적용 방법

## 아키텍처 (중요한 변경사항)

처음엔 Vercel(이 웹앱)이 직접 `http://dspm.dsjs.co.kr:10920`을 호출하는 방식으로 만들었는데,
실제로 배포해보니 **Vercel 서버 → 사내망 방향 요청이 방화벽에 막혀** 8초 타임아웃으로 실패했습니다
(반대로 이 리포의 코드가 있는 환경에서 직접 호출하면 0.1초 만에 정상 응답 — 방화벽이 알려진
클라우드 IP 대역을 막아둔 것으로 보입니다). 그래서 최종 방식은 **방향을 뒤집었습니다**:

- ❌ Vercel(외부) → dspm.dsjs.co.kr(사내망) — 막힘
- ✅ 사내망 PC(로컬 스크립트, 별도 전달한 `dspm-local-sync` 폴더) → dspm.dsjs.co.kr(같은 사내망, 문제없음)
- ✅ 사내망 PC → Firestore(공개 HTTPS) — 인터넷 나가는 방향이라 문제없음
- ✅ Vercel 대시보드는 Firestore를 실시간 구독만 함 — 원래 하던 대로, 변경 없음

**따라서 실제 동기화 실행은 `dspm-local-sync` 폴더(별도 zip)가 담당합니다.** 이 zip(`v2.7-dspm-api-sync`)은
매핑 UI와 데이터 모델만 제공합니다.

## 바뀐 파일 (기존 프로젝트에 그대로 덮어쓰기)

- `src/lib/machine-defs.ts` — `MachineDef`에 `mcno`(실측 설비번호) 필드 추가
- `src/lib/machine-sync.ts` — 상태 매핑 로직 (타입 정의용으로 settings 페이지가 import)
- `src/app/settings/machines/page.tsx` — "실측 설비(MCNO)" 컬럼 + 실측 목록 미리보기
  (Firestore `dspm_live/snapshot` 문서를 실시간 구독 — 이 문서는 `dspm-local-sync`가 채웁니다)
- `src/components/ProcessFlowDiagram.tsx` — 변경 없음(기존 Firestore 구독 그대로)

**`src/app/api/dspm/state/route.ts`, `src/app/api/sync-machines/route.ts`는 지금은 쓰이지 않습니다**
(Vercel에서 사내망 호출이 막혀서). 나중에 방화벽이 뚫리거나 다른 호스팅으로 옮기면 쓸 수 있어서
일단 남겨뒀을 뿐, 지금 동작에는 관여하지 않습니다. 지워도 무방합니다.

`src/lib/machine-service.ts`, `src/app/terminal/[no]/page.tsx` 등 나머지 파일은 안 건드렸습니다 —
매핑 안 된 공정은 지금처럼 사람이 터미널에서 눌러서 입력하는 방식이 그대로 유지됩니다.

## 적용 순서

1. 이 zip 파일들을 기존 프로젝트에 덮어쓰기 → GitHub push → Vercel 재배포.
2. 별도 전달한 `dspm-local-sync.zip`을 사내망 PC(DSPM 서버 자체 또는 상시 켜진 사무실 PC)에 설치하고
   Windows 작업 스케줄러에 등록 — 자세한 건 그 zip 안의 `README.md` 참고.
3. `/settings/machines` 접속 → 화면 하단 "실측 DSPM 설비 목록"에서 실제 보고되는 설비(MCNO/이름/상태)를
   확인하면서, 위 표의 해당 공정 행 "실측 설비(MCNO)" 칸에 번호를 입력 → **저장**.
   - **자동으로 채워둔 매핑은 없습니다.** 아래 "확인이 필요한 부분" 때문에 제가 임의로 추정하면
     실제 현장 데이터가 잘못 표시될 위험이 있어 일부러 비워뒀습니다.
   - 이 미리보기는 `dspm-local-sync`가 최소 1번은 실행돼야 채워집니다(2번을 먼저 해야 함).
4. 매핑을 저장한 공정부터 로컬 스크립트의 다음 실행 주기(기본 1분)에 자동 반영됩니다 — 그 공정의
   `/terminal/[no]` 화면은 이제 안 눌러도 됩니다(화면 자체는 남아있어 필요하면 여전히 수동으로도
   덮어쓸 수 있습니다).

## 확인이 필요한 부분 (실측 API를 조회해본 결과)

지금 실측 API가 보고하는 14개 설비 이름이 앱의 19개 공정명과 자동으로 안 맞아떨어집니다.
직접 확인 부탁드립니다 (설정화면 미리보기에서 이름 보면서 매핑하시면 됩니다):

- **에폭시**: 앱은 1대(`no.9`)인데 실측은 `에폭시_1호기`/`에폭시_2호기` 2대가 보고됩니다. 공정을 하나 더
  나눠야 할 수도 있습니다.
- **코올리나_1/2호기**: 앱의 19개 공정 어디에도 없는 이름입니다. 표지 라인의 다른 공정(예: 낱장재단
  이후 별도 공정)인지 확인 필요합니다.
- **연속지출력(IPHD)**: 실측은 `IPHD양면_2호기`, `IPHD양면-3호기`(2·3호기)로 잡히는데 앱은 1·2호기로
  되어 있습니다. 실제 1호기가 따로 있는지, 아니면 번호만 다르게 붙은 같은 설비인지 확인 필요합니다.
- **미보고 상태(이번 조회에 아예 없었음)**: 날장출력(5·6), 낱장재단(10·11), 날개접지기(16),
  시험지접지기(17), 박스포장(18), 댐지포장(19) — 이 6~8개 공정은 DSPM 터미널 에이전트가 아직
  설치·가동 안 됐거나 다른 이름으로 잡힐 수 있습니다. 해당 라인 PC에 `DSPMTerminalSetup.msi`가
  설치·실행 중인지 확인해 주세요.

## 알아두면 좋은 것 (설계상 트레이드오프)

- **동기화는 `dspm-local-sync`를 돌리는 PC가 켜져 있고 사내망에 연결돼 있는 동안만 작동**합니다.
  그 PC가 꺼지거나 네트워크가 끊기면 그동안은 최신 상태 반영이 멈춥니다(마지막 값이 그대로 남음).
  상시 켜져 있는 PC에서 돌리는 걸 권장합니다.
- **실측 API의 `TM` 값**을 그대로 "상태가 바뀐 시각"으로 신뢰해서 반영합니다. 어떤 설비는 `TM`이
  며칠 전으로 찍혀 있었는데(예: 에폭시_2호기), 이게 "정말 그 상태로 며칠째 멈춰있다"인지 "터미널
  에이전트가 멈춰서 값이 안 올라온다"인지는 이 API만으로는 구분이 안 됩니다. 24시간 넘게 오래된
  값은 로컬 스크립트 로그에 `[stale]`로 표시는 되지만, 반영 자체는 그대로 합니다 — 실제로 오래
  멈춰있는 설비가 많다면 "고착값" 오탐이 섞일 수 있다는 점 감안 부탁드립니다.
- 이 API는 mcno로 필터링을 안 받고 항상 전체 목록을 돌려줍니다.
- Firestore 쓰기 규칙이 지금처럼 인증 없이 열려 있다는 전제로 만들었습니다(기존 터미널 페이지도
  그렇게 동작 중이었습니다).
