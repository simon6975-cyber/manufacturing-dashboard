# v2.7 — 실측 DSPM API 연동 적용 방법

## 바뀐 파일 (기존 프로젝트에 그대로 덮어쓰기)

- `src/lib/machine-defs.ts` — `MachineDef`에 `mcno`(실측 설비번호) 필드 추가
- `src/lib/machine-sync.ts` — **신규**. 실측 API 호출 + Firestore 반영 로직
- `src/app/api/dspm/state/route.ts` — **신규**. 실측 API 읽기전용 프록시(설정화면 미리보기용)
- `src/app/api/sync-machines/route.ts` — **신규**. 실측 API → Firestore 동기화 실행 라우트
- `src/app/settings/machines/page.tsx` — "실측 설비(MCNO)" 컬럼 + 실측 목록 미리보기 추가
- `src/components/ProcessFlowDiagram.tsx` — 15초 주기 자동 동기화 트리거 + "새로고침" 버튼 실동작

`src/lib/machine-service.ts`, `src/app/terminal/[no]/page.tsx` 등 나머지 파일은 안 건드렸습니다 —
매핑 안 된 공정은 지금처럼 사람이 터미널에서 눌러서 입력하는 방식이 그대로 유지됩니다.

## 적용 후 해야 할 일 (순서대로)

1. 파일 덮어쓰기 → GitHub push → Vercel 재배포.
2. `/settings/machines` 접속 → 화면 하단 "실측 DSPM 설비 목록"에서 실제 보고되는 설비(MCNO/이름/상태)를
   확인하면서, 위 표의 해당 공정 행 "실측 설비(MCNO)" 칸에 번호를 입력 → **저장**.
   - **자동으로 채워둔 매핑은 없습니다.** 아래 "확인이 필요한 부분" 때문에 제가 임의로 추정하면
     실제 현장 데이터가 잘못 표시될 위험이 있어 일부러 비워뒀습니다.
3. 매핑을 저장한 공정부터 바로 자동 반영됩니다 — 그 공정의 `/terminal/[no]` 화면은 이제 안 눌러도 됩니다
   (다만 화면 자체는 남아있어 필요하면 여전히 수동으로도 덮어쓸 수 있습니다).
4. (선택) 환경변수 — 없어도 기본값으로 동작합니다.
   - `DSPM_API_URL` — 실측 API 주소 변경 시에만 설정 (기본값: `http://dspm.dsjs.co.kr:10920/api/getmachinestate`)
   - `SYNC_SECRET` — `/api/sync-machines`를 외부에서 직접 호출하는 것을 막고 싶을 때만 설정

## 확인이 필요한 부분 (실측 API를 1회 조회해본 결과)

지금 실측 API가 보고하는 14개 설비 이름이 앱의 19개 공정명과 자동으로 안 맞아떨어집니다.
직접 확인 부탁드립니다 (설정화면 미리보기에서 이름 보면서 매핑하시면 됩니다):

- **에폭시**: 앱은 1대(`no.9`)인데 실측은 `에폭시_1호기`/`에폭시_2호기` 2대가 보고됩니다. 공정을 하나 더
  나눠야 할 수도 있습니다.
- **코올리나_1/2호기**: 앱의 19개 공정 어디에도 없는 이름입니다. 표지 라인의 다른 공정(예: 낱장재단
  이후 별도 공정)인지 확인 필요합니다.
- **연속지출력(IPHD)**: 실측은 `IPHD양면_2호기`, `IPHD양면-3호기`(2·3호기)로 잡히는데 앱은 1·2호기로
  되어 있습니다. 실제 1호기가 따로 있는지, 아니면 번호만 다르게 붙은 같은 설비인지 확인 필요합니다.
- **미보고 상태(이번 조회에 아예 없었음)**: 날장출력(5·6), 낱장재단(10·11), 날개접지기(16),
  시험지접지기(17), 박스포장(18), 댐지포장(19) — 이 6~8개 공정은 DSPM 터미널 에이전트가 아직
  설치·가동 안 됐거나 다른 이름으로 잡힐 수 있습니다. 해당 라인 PC에 `DSPMTerminalSetup.msi`가
  설치·실행 중인지 확인해 주세요.

## 알아두면 좋은 것 (설계상 트레이드오프)

- **동기화 트리거 = 대시보드 화면이 열려 있는 동안만 작동**합니다. `ProcessFlowDiagram`이 15초마다
  자체 서버 라우트(`/api/sync-machines`)를 호출하는 방식이라, 그 화면을 아무도 안 열어놓으면
  동기화도 멈춥니다. 지금처럼 상황판을 상시 띄워두는 운영이면 문제없지만, 화면이 꺼져 있을 때도
  자동 반영이 필요하면 Vercel Cron(Pro 플랜 필요, Hobby는 하루 1회 제한) 또는 무료 외부 크론
  서비스(cron-job.org 등)로 `/api/sync-machines`를 주기 호출하는 방법을 추가로 붙일 수 있습니다 —
  필요하면 말씀해주세요.
- **실측 API의 `TM` 값**을 그대로 "상태가 바뀐 시각"으로 신뢰해서 반영합니다. 어떤 설비는 `TM`이
  며칠 전으로 찍혀 있었는데(예: 에폭시_2호기), 이게 "정말 그 상태로 며칠째 멈춰있다"인지 "터미널
  에이전트가 멈춰서 값이 안 올라온다"인지는 이 API만으로는 구분이 안 됩니다. 24시간 넘게 오래된
  값은 동기화 결과에 `stale`로 표시는 되지만(로그용), 반영 자체는 그대로 합니다 — 실제로 오래
  멈춰있는 설비가 많다면 "고착값" 오탐이 섞일 수 있다는 점 감안 부탁드립니다.
- 이 API는 mcno로 필터링을 안 받고 항상 전체 목록을 돌려줍니다(폴링 부담은 크지 않음).
- Firestore 쓰기 규칙이 지금처럼 인증 없이 열려 있다는 전제로 만들었습니다(기존 터미널 페이지도
  그렇게 동작 중이었습니다). 별도 인증을 붙이신 상태라면 서버 라우트 쪽에 Admin SDK 자격증명이
  추가로 필요합니다.
